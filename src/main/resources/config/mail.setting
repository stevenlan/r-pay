# 邮件服务器的SMTP地址，可选，默认为smtp.<发件人邮箱后缀>可以自定义
host = smtpdm-ap-southeast-1.aliyun.com
# 邮件服务器的SMTP端口，可选，默认25 465
port = 25
# 发件人邮箱地址
from = services@mail1.reliancepay.net
# 用户名，默认为发件人邮箱前缀
user = services@mail1.reliancepay.net
# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码 比如QQ邮箱）
pass = sZgi48z2bQ
#使用 STARTTLS安全连接，STARTTLS是对纯文本通信协议的扩展
starttlsEnable = true
# 需要设置为false 否则QQ邮箱测试邮件发送报错
sslEnable = false